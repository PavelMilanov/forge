services:
  alpine:
    image: alpine:{{ tag "test_alpine" }}
    container_name: alpine
    restart: unless-stopped

  nginx:
    image: nginx:{{ tag "test_nginx" }}
    container_name: nginx
    restart: unless-stopped

  postgres:
    image: postgres:{{ tag "test_postgres" }}
    container_name: postgres
    restart: unless-stopped
