services:
  app:
    image: myimage/backend:{{ tag "app" }}
    container_name: app
    restart: unless-stopped

  app2:
    image: myimage/frontend:{{ tag "app" }}
    container_name: app2
    restart: unless-stopped

  redis:
    image: redis:latest
    container_name: redis
    restart: unless-stopped
